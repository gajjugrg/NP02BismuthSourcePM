[project]
name = "np02-purity-monitor-analysis"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "pandas",
  "scipy",
  "matplotlib",
]

[tool.setuptools]
# This repo is primarily script-based; packaging is optional.
